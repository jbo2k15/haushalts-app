-- RedefineTables
PRAGMA defer_foreign_keys=ON;
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Task" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "type" TEXT NOT NULL,
    "priority" TEXT NOT NULL DEFAULT 'normal',
    "weekdays" TEXT,
    "fixedWeekday" INTEGER,
    "fixedDayOfMonth" INTEGER,
    "monthlyInterval" INTEGER,
    "nextDueMonth" TEXT,
    "dueDate" TEXT,
    "sortOrder" INTEGER NOT NULL DEFAULT 0,
    "isActive" BOOLEAN NOT NULL DEFAULT 1,
    "isAutoGenerated" BOOLEAN NOT NULL DEFAULT 0,
    "allowMultiple" BOOLEAN NOT NULL DEFAULT 0,
    "weatherDependent" BOOLEAN NOT NULL DEFAULT 0,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO "new_Task" ("id", "title", "type", "priority", "weekdays", "fixedWeekday", "fixedDayOfMonth", "dueDate", "sortOrder", "isActive", "isAutoGenerated", "allowMultiple", "weatherDependent", "createdAt") SELECT "id", "title", "type", "priority", "weekdays", "fixedWeekday", "fixedDayOfMonth", "dueDate", "sortOrder", "isActive", "isAutoGenerated", "allowMultiple", "weatherDependent", "createdAt" FROM "Task";
DROP TABLE "Task";
ALTER TABLE "new_Task" RENAME TO "Task";
PRAGMA foreign_keys=ON;
PRAGMA defer_foreign_keys=OFF;
